# Car Rental Database

A relational database project created in Microsoft SQL Server for a fictional car rental company.

The project focuses on database design, synthetic data generation, SQL data analysis and preparing data for visualization in Power BI.

## Project Overview

The database stores information about:

- cars available for rental,
- customers,
- rental locations,
- rental dates,
- completed rentals and their revenue.

The dataset is synthetically generated for educational and portfolio purposes.

## Database Structure

The database consists of the following main tables:

### Cars
Stores information about the vehicle fleet, including:
- brand,
- model,
- production year,
- daily rental rate.

### Customers
Stores customer information such as:
- first name,
- last name,
- phone number,
- email address.

### RentalLocations
Contains information about rental offices and their locations.

### RentalDates
Stores rental date information, including:
- start date,
- end date,
- month,
- year.

### Rentals
The main transaction table connecting cars, customers, locations and dates.

It also stores the total value of each rental.

## Data Model

The `Rentals` table acts as the central transaction table.

Main relationships:

Cars (1) -------- (*) Rentals  
Customers (1) --- (*) Rentals  
RentalLocations (1) --- (*) Rentals  
RentalDates (1) --- (*) Rentals  

Primary and foreign keys are used to maintain relationships between the tables.

## SQL Features

The project demonstrates the use of several SQL Server features:

- Primary and foreign keys
- Stored procedures
- Common Table Expressions (CTEs)
- Aggregate functions
- GROUP BY
- Window functions
- LAG
- Moving averages
- PIVOT
- CUBE
- User-defined functions
- Date functions
- Synthetic data generation

## Data Generation

Stored procedures are used to generate sample data for the database.

The generated dataset includes cars, customers, rental locations, dates and rental transactions.

The data is synthetic and was created to simulate a car rental business for analytical purposes.

## Data Analysis

The repository contains several analytical SQL queries.

### Monthly Revenue Change

Calculates monthly revenue and compares it with the previous month using the `LAG()` window function.

### Moving Average

Calculates a moving average of monthly revenue to make revenue trends easier to analyze.

### Revenue Pivot

Uses `PIVOT` to present monthly revenue in a year/month format.

### Revenue by Brand and Year

A SQL function calculates revenue generated by a selected car brand in a selected year.

## Power BI

The database can be used as a data source for Power BI.

The Power BI model can use `Rentals` as the central fact table and the remaining tables as dimensions.

Example visualizations include:

- total revenue,
- number of rentals,
- average rental value,
- monthly revenue,
- revenue moving average,
- revenue by car brand,
- revenue by rental location,
- rental activity by year and month.

This allows the SQL database to be used not only for data storage, but also for business intelligence and reporting.

## Technologies

- Microsoft SQL Server
- T-SQL
- SQL Server Management Studio
- Power BI

## Repository Files

`CreateTables.sql`  
Creates the database tables and relationships.

`FillTablesProcedures.sql`  
Contains stored procedures used to generate sample data.

`FillTablesExec.sql`  
Executes the procedures used to populate the database.

`DropTables.sql`  
Removes the database tables.

`MonthlyRevenueChange.sql`  
Calculates month-to-month revenue changes.

`MovingAverage.sql`  
Calculates the moving average of monthly revenue.

`RevenuePivot.sql`  
Creates a pivot table containing revenue by year and month.

`ProfitByBrandAndYearFunction.sql`  
Contains a function for calculating revenue for a selected brand and year.

## How to Run

1. Create a new database in Microsoft SQL Server.
2. Run `CreateTables.sql`.
3. Run `FillTablesProcedures.sql`.
4. Run `FillTablesExec.sql`.
5. Execute the analytical SQL scripts to explore the generated data.
6. Optionally connect the database to Power BI to create reports and visualizations.

## Purpose

The main purpose of this project is to practice and demonstrate skills in:

- relational database design,
- T-SQL,
- data generation,
- analytical SQL,
- window functions,
- data aggregation,
- Power BI data modeling and visualization.